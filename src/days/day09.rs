use crate::{Solution, SolutionPair};
use std::fs::read_to_string;

fn part1(_data: &str) -> u64 {
    0
}
fn part2(_data: &str) -> u64 {
    0
}

pub fn solve() -> SolutionPair {
    let sol1 = part1("");
    let sol2 = part2("");

    (Solution::from(sol1), Solution::from(sol2))
}
